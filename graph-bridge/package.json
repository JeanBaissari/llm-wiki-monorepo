{
  "name": "@baissari/llm-wiki-graph-bridge",
  "version": "0.2.5",
  "private": true,
  "description": "Graph merger bridge for LLM Wiki — unifies wikilink and derived graph layers",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@baissari/llm-wiki-shared-types": "file:../../packages/shared-types",
    "graphology": "^0.26.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  }
}
